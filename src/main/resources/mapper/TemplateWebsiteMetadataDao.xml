<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.renren.cms.dao.TemplateWebsiteMetadataDao">


	<!-- 可根据自己的需求，是否要使用 -->
	<resultMap type="io.renren.cms.entity.TemplateWebsiteMetadataEntity" id="templateWebsiteMetadataMap">
		<result property="id" column="id" />
		<result property="ctime" column="ctime" />
		<result property="utime" column="utime" />
		<result property="metadata" column="metadata" />
		<result property="memberId" column="member_id" />
		<result property="operator" column="operator" />
	</resultMap>

	<select id="queryObjectByMemberId" resultType="io.renren.cms.entity.TemplateWebsiteMetadataEntity">
		select * from tb_template_website_metadata where member_id = #{value}
	</select>

	<select id="queryObject" resultType="io.renren.cms.entity.TemplateWebsiteMetadataEntity">
		select * from tb_template_website_metadata where id = #{value}
	</select>

	<select id="queryList" resultType="io.renren.cms.entity.TemplateWebsiteMetadataEntity">
		select * from tb_template_website_metadata where 1 = 1
		<if test="memberId != null and memberId != ''"> and `member_id` = #{memberId}</if>
		<if test="operator != null and operator != ''"> and `operator` = #{operator}</if>

		<if test="sdate != null and sdate != '' and edate != null and edate != ''">
			and (ctime between CONCAT(#{sdate},' 00:00:00') and CONCAT(#{edate},' 23:59:59'))
		</if>

		<choose>
			<when test="sidx != null and sidx.trim() != ''">
				order by ${sidx} ${order}
			</when>
			<otherwise>
				order by id desc
			</otherwise>
		</choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>

	<select id="queryTotal" resultType="int">
		select count(id) from tb_template_website_metadata where 1 = 1
		<if test="memberId != null and memberId != ''"> and `member_id` = #{memberId}</if>
		<if test="operator != null and operator != ''"> and `operator` = #{operator}</if>

		<if test="sdate != null and sdate != '' and edate != null and edate != ''">
			and (ctime between CONCAT(#{sdate},' 00:00:00') and CONCAT(#{edate},' 23:59:59'))
		</if>
	</select>

	<insert id="save" parameterType="io.renren.cms.entity.TemplateWebsiteMetadataEntity" useGeneratedKeys="true" keyProperty="id">
		insert into tb_template_website_metadata
		(
		`id`,
		`ctime`,
		`utime`,
		`metadata`,
		`member_id`,
		`operator`
		)
		values
		(
		#{id},
		#{ctime},
		#{utime},
		#{metadata},
		#{memberId},
		#{operator}
		)
	</insert>

	<update id="update" parameterType="io.renren.cms.entity.TemplateWebsiteMetadataEntity">
		update tb_template_website_metadata
		<set>
			<if test="ctime != null">`ctime` = #{ctime}, </if>
			<if test="utime != null">`utime` = #{utime}, </if>
			<if test="metadata != null">`metadata` = #{metadata}, </if>
			<if test="memberId != null">`member_id` = #{memberId}, </if>
			<if test="operator != null">`operator` = #{operator}</if>
		</set>
		where id = #{id}
	</update>

	<!-- 物理删除 -->
	<delete id="delete">
		delete from tb_template_website_metadata where id = #{value}
	</delete>

	<!-- 批量物理删除 -->
	<delete id="deleteBatch">
		delete from tb_template_website_metadata where id in
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>

	<!-- 逻辑删除 -->
	<update id="logicDel">
		update tb_template_website_metadata set is_del = 't' where id = #{value}
	</update>

	<!-- 批量逻辑删除 -->
	<update id="logicDelBatch">
		update tb_template_website_metadata
		set is_del = 't'
		where id in
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</update>

</mapper>