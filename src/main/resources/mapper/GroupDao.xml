<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.renren.cms.dao.GroupDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="io.renren.cms.entity.GroupEntity" id="groupMap">
        <result property="id" column="id"/>
        <result property="groupId" column="group_id"/>
        <result property="openId" column="open_id"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

	<select id="queryObject" resultType="io.renren.cms.entity.GroupEntity">
		select * from tb_group where id = #{value}
	</select>

	<select id="queryList" resultType="io.renren.cms.entity.GroupEntity">
		select * from tb_group where 1=1 
		<if test="openId != null and openId != null">
			and open_id= #{openId}
		</if>
		<if test="groupId != null and groupId != null">
			and group_id= #{groupId}
		</if>
        <choose>
            <when test="sidx != null and sidx.trim() != ''">
                order by ${sidx} ${order}
            </when>
			<otherwise>
                order by id desc
			</otherwise>
        </choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
 	<select id="queryTotal" resultType="int">
		select count(*) from tb_group 
	</select>
	 
	<insert id="save" parameterType="io.renren.cms.entity.GroupEntity" useGeneratedKeys="true" keyProperty="id">
		insert into tb_group
		(
			`group_id`, 
			`open_id`, 
			`create_time`
		)
		values
		(
			#{groupId}, 
			#{openId}, 
			#{createTime}
		)
	</insert>
	 
	<update id="update" parameterType="io.renren.cms.entity.GroupEntity">
		update tb_group 
		<set>
			<if test="groupId != null">`group_id` = #{groupId}, </if>
			<if test="openId != null">`open_id` = #{openId}, </if>
			<if test="createTime != null">`create_time` = #{createTime}</if>
		</set>
		where id = #{id}
	</update>
	
	<delete id="delete">
		delete from tb_group where id = #{value}
	</delete>
	
	<delete id="deleteBatch">
		delete from tb_group where id in 
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>

</mapper>